Task #3:


In this task, the program will read the series of integers thats in the input text file, in which can be any size and must be positive integers. The format for each row of the matrix is integer, space then integer. The program will then output the number of nodes, the minimum length, and the optimal path that it took, meaning the visited nodes for the accurate optimization. 